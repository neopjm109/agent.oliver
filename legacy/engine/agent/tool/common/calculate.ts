import { Tool } from "../types";

const calculateTool = (): Tool => {
  return {
    definition: {
      name: "calculate",
      description: "",
    },
    execute: async () => {},
  };
};

export default calculateTool;
