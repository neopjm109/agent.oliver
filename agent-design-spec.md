# 로컬 8B 기반 업무 보조 에이전트 — 설계 명세

> **이 문서의 용도**: Claude Code에 전달하여 구현을 맡기기 위한 설계 스펙.
> **핵심 원칙**: 코드가 뼈대(흐름 제어), LLM이 관절(국소 판단). LLM은 절대 흐름을 제어하지 않는다.

---

## 0. 설계 전제

| 항목 | 결정 |
|---|---|
| 모델 급 | ~8B (Q4~Q5 양자화, 로컬) |
| 자율성 레벨 | **Level 0 — 고정 워크플로** (입구에 라우팅 1노드만 허용) |
| 실행 구조 | 상태 기계(state machine). ReAct 자율 루프 **금지** |
| LLM 역할 | 정규화 / 분류 / 추출 / 모호성 감지 |
| LLM 비역할 | 계산, 날짜 연산, 순서 제어, 실행 결정, 부작용 유발 |

### 설계 불변식 (Invariants)

구현 중 이 5가지는 어떤 경우에도 위반하지 않는다.

1. **LLM 출력은 항상 스키마 검증을 통과한 뒤에만 다음 노드로 전달된다.**
2. **노드는 다음 노드를 지정할 수 없다.** 전이는 오케스트레이터가 결정한다.
3. **부작용이 있는 동작은 LLM이 최종 실행권을 갖지 않는다.** 제안 → 승인 → 코드 실행.
4. **모든 노드는 컨텍스트를 새로 조립한다.** 대화 히스토리를 append하지 않는다.
5. **결정적으로 계산 가능한 값은 LLM에게 묻지 않는다.**

---

## 1. 전체 아키텍처

```
사용자 입력
    │
    ▼
┌─────────────────┐
│ 전처리 (코드)     │  날짜/금액/ID/경로/파일명 사전 추출 → entities
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ [LLM] 의도 라우팅 │  닫힌 집합 분류. 낮은 확신도 → clarify
└─────────────────┘
    │
    ▼
┌─────────────────┐
│ 오케스트레이터     │  워크플로 선택 · 전이 · 스텝 제한 · 로깅
└─────────────────┘
    │
    ├──▶ [LLM] 슬롯 추출 ──▶ 검증 ──▶ (부족) ──▶ 되묻기
    │
    ├──▶ 도구 실행 (코드) ──▶ 결과 구조화
    │
    ├──▶ [LLM] 요약/작성 ──▶ 검증
    │
    └──▶ 승인 게이트 ──▶ 실행 ──▶ 감사 로그
```

---

## 2. 디렉터리 구조

```
agent/
├── core/
│   ├── orchestrator.py      # 상태 기계 실행기. 전이 · 스텝 제한 · 에스컬레이션
│   ├── node.py              # Node 추상 클래스 (계약 정의)
│   ├── context.py           # 노드별 컨텍스트 조립 (누적 금지)
│   └── errors.py            # ValidationError / EscalationRequired / NeedsClarification
├── nodes/
│   ├── llm/                 # LLM 호출 노드. 각 파일 = 1노드 = 1판단
│   │   ├── route_intent.py
│   │   ├── extract_slots.py
│   │   └── compose_text.py
│   └── code/                # 결정적 노드
│       ├── parse_entities.py
│       └── ...
├── llm/
│   ├── client.py            # 추론 백엔드 어댑터 (교체 가능해야 함)
│   ├── grammar.py           # 스키마 → GBNF 변환
│   └── prompts/             # 노드별 프롬프트 (코드와 분리)
├── tools/
│   ├── registry.py          # 도구 등록 + 단계별 노출 필터
│   └── impl/
├── schemas/                 # Pydantic 모델. 노드 입출력 계약의 단일 원천
├── workflows/               # 워크플로 정의 (선언적: YAML 또는 dataclass)
├── audit/
│   └── logger.py            # 구조화 로그 (JSONL)
└── eval/
    ├── dataset.jsonl        # 실제 업무 요청 50~100건
    └── run_eval.py          # 스텝 정확도 / 종단 성공률 분리 측정
```

**구조상 요구사항**
- `llm/client.py`는 백엔드 교체가 가능한 인터페이스여야 한다 (로컬 → 상위 모델 에스컬레이션에 필요).
- 프롬프트는 코드에 인라인하지 않는다. 8B는 프롬프트 변경에 민감해서 버전 관리 대상이다.
- `schemas/`가 노드 간 계약의 유일한 출처. 노드는 서로의 내부를 모른다.

---

## 3. 노드 계약

모든 노드는 동일한 형태를 갖는다.

```python
class Node(ABC):
    name: str
    input_schema: Type[BaseModel]
    output_schema: Type[BaseModel]
    max_retries: int = 1          # 검증 실패 재시도. 2회 초과 금지

    @abstractmethod
    def run(self, inp: BaseModel, ctx: Context) -> BaseModel: ...
```

### LLM 노드 규칙

| 규칙 | 이유 |
|---|---|
| 1노드 = 1판단 | 8B는 복합 판단에서 급격히 무너짐 |
| 출력은 constrained decoding으로 강제 | 형식 오류를 사실상 0으로 |
| 프롬프트에 이전 원문 히스토리 금지 | 구조화된 값만 전달 |
| 출력 스키마는 평평하게 | 중첩·optional·union은 소형 모델에 독 |
| 가능하면 생성 대신 열거형 선택 | "고르기"가 "생성"보다 훨씬 쉬움 |
| `confidence` 또는 `needs_clarification` 필드 필수 | 되묻기 능력이 이 시스템의 핵심 가치 |

### 실패 처리

```
스키마 검증 실패
  └─ 1회 재시도 (에러 메시지를 프롬프트에 포함)
      └─ 재실패 → EscalationRequired
                    ├─ 상위 모델 (설정 시)
                    └─ 사용자에게 되묻기
```

무한 재시도는 금지한다. 8B는 같은 실수를 반복하지, 스스로 접근을 바꾸지 않는다.

---

## 4. 컨텍스트 조립 정책

**절대 하지 않을 것**: `messages.append(...)`로 히스토리를 누적.

각 노드는 실행 시점에 필요한 것만으로 프롬프트를 새로 만든다.

```python
def build(self, node_name: str) -> Prompt:
    return Prompt(
        system=load_prompt(node_name),        # 고정 (KV 캐시 재사용)
        task=self.slots_for(node_name),       # 구조화된 값만
        entities=self.entities,               # 코드가 추출한 확정값
        # 이전 노드의 LLM 원문 출력은 포함하지 않는다
    )
```

**목표: 노드당 프롬프트 2k 토큰 이하.** 컨텍스트가 짧을수록 8B의 정확도는 눈에 띄게 올라간다.

시스템 프롬프트를 노드별로 고정하면 KV 캐시를 재사용할 수 있어 프리필 지연이 크게 줄어든다.

---

## 5. 도구 설계

```python
@tool(
    name="search_internal_docs",     # 이름이 곧 설명
    exposed_in=["research"],         # 이 단계에서만 노출
)
def search_internal_docs(keyword: str, doc_type: Literal["spec","report","memo"]) -> list[Doc]:
    ...
```

**제약**
- **동시 노출 5개 이하.** 초과하면 워크플로 단계를 쪼갠다.
- 파라미터는 평면 구조. 중첩 객체 금지.
- 자유 문자열보다 `Literal` 열거형 우선.
- 도구 출력은 원문이 아니라 요약·구조화해서 컨텍스트에 넣는다.

---

## 6. 안전장치 (필수 구현)

| 장치 | 사양 |
|---|---|
| 스텝 하드 리밋 | 워크플로당 최대 8스텝. 초과 시 무조건 중단 + 사용자 보고 |
| 승인 게이트 | 메일 발송 · 파일 삭제 · 외부 쓰기는 dry-run 결과를 보여주고 사람 승인 후 실행 |
| 멱등성 | 모든 부작용 동작에 `request_key` 부여. 재시도가 중복 실행이 되지 않게 |
| 롤백 | 되돌릴 수 없는 동작은 실행 전 상태 스냅샷 |
| 타임아웃 | 노드별 개별 타임아웃. 전체 워크플로 타임아웃 별도 |
| 감사 로그 | 아래 참조 |

### 감사 로그 스키마 (JSONL)

```json
{
  "trace_id": "...",
  "step": 3,
  "node": "extract_slots",
  "decided_by": "llm-8b",
  "model": "...",
  "input_digest": "sha256:...",
  "output": {...},
  "confidence": 0.72,
  "validation": "passed_after_retry",
  "latency_ms": 1840,
  "human_corrected": null
}
```

`decided_by`와 `human_corrected`가 특히 중요하다. **어느 노드를 상위 모델로 올려야 하는지를 감으로가 아니라 데이터로 판단**하기 위한 필드다.

---

## 7. 업그레이드 경로 (설계 단계에서 미리 확보)

노드 경계가 깨끗하면 나중에 특정 노드만 교체할 수 있다. 이를 위해:

```yaml
# config/models.yaml
nodes:
  route_intent:   { backend: local_8b }
  extract_slots:  { backend: local_8b }
  compose_text:   { backend: local_8b, escalate_to: remote_large }
default_escalation: remote_large
```

- 모델 선택은 **설정 파일에서만** 이루어진다. 노드 코드는 어떤 모델이 자기를 실행하는지 몰라야 한다.
- 이 구조가 있으면 나중에 `compose_text`만 상위 모델로 올리는 게 설정 한 줄이 된다. 없으면 전체를 다시 짜야 한다.

---

## 8. 평가 (구현과 동시에 만들 것)

8B는 프롬프트 한 줄에도 성능이 출렁이므로, 평가 세트 없이 튜닝하면 개선인지 퇴보인지 알 수 없다.

**측정 항목 (반드시 분리)**

1. **노드별 스텝 정확도** — 어느 노드가 병목인지 특정하기 위함
2. **종단 성공률** — 실사용 체감
3. **모호성 감지 재현율** — 되물었어야 할 때 되물었는가 (이 시스템에서 가장 중요한 지표)
4. **오탐율** — 명확한 요청인데 불필요하게 되묻는 비율
5. **노드별 p50 / p95 지연**

**종단 성공률만 보면 범인을 절대 못 찾는다.** 스텝 단위 로그가 평가의 전제다.

---

## 9. 구현 순서

Claude Code에 맡길 때 이 순서를 지킬 것. 뼈대 없이 노드부터 만들면 흐름 제어가 프롬프트에 스며든다.

1. `schemas/` — 입출력 계약부터 확정
2. `core/` — 오케스트레이터 + Node 추상 + 컨텍스트 조립. **LLM 없이 목(mock) 노드로 전이 테스트 통과**
3. `llm/client.py` + `grammar.py` — constrained decoding 확인
4. 라우팅 노드 1개 → 평가 세트로 측정
5. 워크플로 1개를 종단까지 완성
6. 감사 로그 · 승인 게이트
7. 나머지 워크플로 확장

**2번을 LLM 없이 통과시키는 게 핵심.** 여기서 흐름이 코드에 온전히 들어가 있으면 이후 설계가 무너지지 않는다.

---

## 10. 안티패턴 체크리스트

구현 리뷰 시 이 항목들을 확인한다.

- [ ] LLM이 다음 노드를 고르고 있지 않은가
- [ ] 대화 히스토리가 누적되고 있지 않은가
- [ ] 정규식으로 LLM의 **의미 오류를 사후 교정**하고 있지 않은가 (조용히 틀린 값이 통과하는 최악의 패턴)
- [ ] 날짜·금액·수량을 LLM이 계산하고 있지 않은가
- [ ] 한 노드가 두 가지 이상을 판단하고 있지 않은가
- [ ] 재시도에 상한이 없지 않은가
- [ ] 부작용 동작에 승인 게이트가 빠지지 않았는가
- [ ] 프롬프트가 코드에 인라인되어 있지 않은가

---

## 부록: 열린 결정 사항

구현 전에 확정 필요.

| 항목 | 선택지 |
|---|---|
| 추론 백엔드 | llama.cpp(GBNF) / vLLM(XGrammar) / Ollama — 백엔드마다 제약 문법 지원이 다름 |
| 워크플로 정의 방식 | YAML 선언형 vs 파이썬 dataclass |
| 승인 UI | CLI 확인 / 웹 / 메신저 봇 |
| 에스컬레이션 대상 | 상위 로컬 모델 vs 원격 API vs 사람만 |
| 상태 영속화 | 인메모리 / SQLite (장시간 워크플로 시 필요) |
