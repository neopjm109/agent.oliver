import { Tool } from "../types";

const moveFileTool = (): Tool => {
  return {
    definition: {
      name: "move_file",
      description: "",
    },
    execute: async () => {},
  };
};

export default moveFileTool;
