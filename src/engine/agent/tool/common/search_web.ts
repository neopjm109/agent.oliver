import { Tool } from "../types";

const searchWebTool = (): Tool => {
  return {
    definition: {
      name: "search_web",
      description: "",
    },
    execute: async () => {},
  };
};

export default searchWebTool;
