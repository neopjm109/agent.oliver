import { Tool } from "../types";

const analyzeTool = (): Tool => {
  return {
    definition: {
      name: "analyze",
      description: "",
    },
    execute: async () => {},
  };
};

export default analyzeTool;
