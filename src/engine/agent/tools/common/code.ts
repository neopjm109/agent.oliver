import { Tool } from "../types";

const codeTool: Tool = {
  definition: {
    name: "code",
    description: "",
    intents: ["generate"],
  },
  execute: async () => {},
};

export default codeTool;
